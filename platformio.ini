; Ambiente de teste em host (sem hardware).
; O firmware ESP32 continua sendo compilado/gravado via Arduino IDE, intocado por este arquivo.
[env:native]
platform = native
test_framework = unity
build_flags = -std=gnu++17
